[build-system]
requires = [
  "setuptools >= 40.9.0",
  "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "qpi_seg"
version = "0.0.0"

[tool.setuptools]
package-dir = {"" = "qpi_seg"}